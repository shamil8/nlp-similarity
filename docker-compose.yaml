version: "3.8"

services:
  # Python container
  py:
    build:
      context: .
      dockerfile: docker/Dockerfile
    image: intr_py
    container_name: intr_py
    ports:
      - 5000:5000
    volumes:
      - ./:/app
