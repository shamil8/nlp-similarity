version: "3.8"

services:
  # Python container
  py:
    build:
      context: .
      dockerfile: Dockerfile
    image: intr_py
    container_name: intr_py
    ports:
      - 5000:5000
    volumes:
      - ./nlp_py/:/app/nlp_py
      - ./api.py:/app/api.py
