version: "3.8"

services:
  # Python container
  py:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: intr_dev_py
    ports:
      - 5001:5000
    volumes:
      - ./nlp_py/:/app/nlp_py
      - ./rating/:/app/rating
      - ./connector/:/app/connector
      - ./api.py:/app/api.py

#  # MySQL container
#  mysql:
#    image: mysql
#    container_name: intr_mysql
#    command:
#      - '--character-set-server=utf8mb4'
#      - --default-authentication-plugin=mysql_native_password
#    restart: always
#    environment:
#      MYSQL_ROOT_PASSWORD: root
#    ports:
#      - 3306:3306
#
#  # Adminer for MySQL and MongoDB
#  adminer:
#    image: adminer
#    container_name: intr_adminer
#    restart: always
#    ports:
#      - 8086:8080  # port for adminer 8086
